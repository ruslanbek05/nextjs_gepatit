# nextjs_gepatit

## Check Node and npm
node -v
npm -v

To check if your installed modules match your package.json:
npm ls --depth=0

To ensure your environment matches the package.json exactly (install missing, remove extraneous, update as needed):
npm ci

npm prune removes extraneous packages (those not listed in package.json).
npm prune

npm install installs missing packages and updates as needed.
npm install


3. **Run the development server**

npm run lint
npm run build

npm run dev
npx next dev -p 3006

# caprover 
[instructions-caprover.md](file:///D:/projects/home_hosting/12%20projects%20secret%20files/gepatit/instructions-caprover.md)
